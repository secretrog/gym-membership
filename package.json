{
  "name": "gym-management-root",
  "version": "1.0.0",
  "description": "Root package to help Railway deploy the backend folder automatically",
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npm run build",
    "start": "cd backend && npx prisma migrate deploy && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
